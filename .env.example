# Supabase (required for the app)
# Get these from your Supabase project: Project Settings → API
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-public-key

# Optional reference
VITE_SUPABASE_PROJECT_ID=your-project-ref

# Resend (for Edge Functions: invoices + review requests)
# Set in Supabase Dashboard → Project Settings → Edge Functions → Secrets
# RESEND_API_KEY=re_xxxx
# Without it, "Send invoice" and "Mark as sent" (review) still work but won't send email; you can share links manually.

# Stripe (subscription billing)
# Frontend: add to .env for checkout to work
# VITE_STRIPE_PUBLISHABLE_KEY=pk_test_xxx  (optional, for Stripe.js if needed later)
VITE_STRIPE_PRICE_MONTHLY=price_xxx
VITE_STRIPE_PRICE_ANNUAL=price_xxx
# Edge Function secrets (Supabase Dashboard → Edge Functions → Secrets):
# STRIPE_SECRET_KEY=sk_test_xxx
# STRIPE_WEBHOOK_SECRET=whsec_xxx  (from Stripe Dashboard → Webhooks)
